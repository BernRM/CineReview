# Provisionamento automático do datasource do Loki no Grafana.
# Montado em /etc/grafana/provisioning/datasources/loki.yaml via config do Swarm.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
